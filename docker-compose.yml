services:
  mysql:
    container_name: guide-mysql
    image: mysql:latest
    environment:
      - MYSQL_DATABASE=mydatabase
      - MYSQL_PASSWORD=server
      - MYSQL_ROOT_PASSWORD=server
      - MYSQL_USER=joao
    ports:
      - 3306:3306